// Placeholder — `flutter create .` scaffolds a counter-app smoke test here
// by default, which doesn't apply to this app (no `MyApp`/counter widget).
// Real widget tests aren't set up yet; pumping the actual `App` widget needs
// bootstrap dependencies mocked first (SharedPreferences, Isar, auth
// session) — see lib/bootstrap.dart.
import 'package:flutter_test/flutter_test.dart';

void main() {
  test('placeholder', () {
    expect(1 + 1, 2);
  });
}
